apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authelia-db-data
  namespace: authelia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
