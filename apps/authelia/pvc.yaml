apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authelia-config
  namespace: authelia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: authelia-config-pv
  resources:
    requests:
      storage: 1Gi
