apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallabag-db-data
  namespace: wallabag
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
