apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ntfy-data
  namespace: ntfy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: ntfy-data-pv
  resources:
    requests:
      storage: 1Gi
