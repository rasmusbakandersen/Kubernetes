apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: omni-tools
  namespace: argocd
spec:
  project: default
  source:
    repoURL: ssh://git@gitea.sanxer.dk:2222/admin/Kubernetes.git
    targetRevision: HEAD
    path: apps/omni-tools
  destination:
    server: https://kubernetes.default.svc
    namespace: omni-tools
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
