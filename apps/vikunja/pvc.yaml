apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vikunja-files
  namespace: vikunja
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: vikunja-files-pv
  resources:
    requests:
      storage: 1Gi
